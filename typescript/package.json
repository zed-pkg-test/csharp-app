{
  "name": "@zed-pkg/client",
  "version": "0.1.0",
  "description": "TypeScript SDK for the zed-pkg registry (zero runtime deps)",
  "license": "MIT",
  "repository": "github:zed-pkg/zed-clients",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p .",
    "test": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
